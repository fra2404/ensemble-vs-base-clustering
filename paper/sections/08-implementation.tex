\section{Implementation Details}


Full code for clustering algorithms, ensembles, stability analysis, and multi-dataset evaluation is available at \url{https://github.com/fra2404/ensemble-vs-base-clustering}.

\subsection{Multi-Dataset Pipeline}

The pipeline supports three datasets with automated preprocessing: robust data loading (format/missing values), outlier removal (IQR), feature scaling (RobustScaler), and flexible dataset selection for comparison.

\subsection{Ensemble Method Algorithms}

\lstset{basicstyle=\footnotesize\ttfamily,breaklines=true,xleftmargin=1.5em}
\begin{lstlisting}[language=Python,caption={Core steps for CSPA ensemble and bootstrap stability analysis}]
# CSPA Ensemble (core steps)
for i, j in all pairs:
    similarity[i, j] = mean([lbl[i]==lbl[j] for lbl in base_labels_list])
final_labels = SpectralClustering(n_clusters_final, affinity='precomputed').fit_predict(similarity)

# Bootstrap Stability (core steps)
base_labels = clustering_func(X)
for _ in range(n_boot):
    idx = random_choice(n_samples, n_samples, replace=True)
    labels_boot = clustering_func(X[idx])
    ari_scores.append(adjusted_rand_score(base_labels[idx], labels_boot))
    vi_scores.append(variation_of_information(base_labels[idx], labels_boot))
\end{lstlisting}